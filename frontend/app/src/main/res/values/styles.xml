<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Splash screen theme -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@xml/splash</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorAccentDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Remove default action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Prevent text from being capitalized -->
        <item name="textAllCaps">false</item>

        <item name="editTextStyle">@style/AppTheme.EditText</item>
        <item name="buttonStyle">@style/AppTheme.Button</item>
        <item name="android:textViewStyle">@style/AppTheme.TextView</item>
    </style>

    <!-- Edit Text styles -->
    <style name="AppTheme.EditText" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">@color/gray</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="android:drawablePadding">10dp</item>
    </style>

    <!--Floating label text style-->
    <style name="AppTheme.HintText" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <!-- Button styles -->
    <style name="AppTheme.Button" parent="Widget.AppCompat.Button">
        <item name="colorButtonNormal" tools:targetApi="21">@color/colorAccent</item>
        <item name="colorControlHighlight" tools:targetApi="21">@color/colorAccentDark</item>
        <item name="android:height">58dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Text View style -->
    <style name="AppTheme.TextView" parent="android:Widget.TextView">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Tab Layout styles -->
    <style name="AppTheme.TabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/colorAccent</item>
        <item name="tabTextAppearance">@style/TabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/colorAccent</item>
    </style>
    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="textAllCaps">false</item>
    </style>

    <!-- Bottom Navigation -->
    <style name="AppTheme.BottomNavigation">
        <item name="itemIconTint">@xml/selector_bottom_navigation</item>
        <item name="itemTextColor">@xml/selector_bottom_navigation</item>
    </style>

</resources>
